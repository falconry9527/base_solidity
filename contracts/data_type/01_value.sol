// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

contract testInt {

    // int 类型
    function testInt() public pure  () {

        uint256 x=8;
        uint256 y=10;
        z = x + y;
        z = x / y;

        z = x / y;
        z = x << y;
        z = x << y;

        uint256 x = 1;
        uint256 y = ++x; // c = ++a;
    }


    
}
